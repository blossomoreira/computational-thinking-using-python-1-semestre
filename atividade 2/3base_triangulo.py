# Tendo a base e a altura de um triângulo, calcule sua área por meio da
# fórmula: area = (base x altura)/2.

altura = float(input("Digite a altura do triângulo: "))
base = float(input("Digite a base do triângulo: "))

area = (base * altura) / 2

print(("Sua área é: "),(area))
